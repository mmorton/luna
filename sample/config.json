{
    "settings": {
        "controllerRoot": "/controllers",
        "layoutRoot": "/views/layout",
        "viewRoot": "/views"
    },
    "routes": [{
        "for": "{controller}/{action}/{id}",
        "defaults": {
            "action": "index",
            "id": ""
        }
    },{
        "for": "full/{controller}/{action}/{id}/{*other}",
        "defaults": {
            "action": "index",
            "id": ""
        }
    }]
}